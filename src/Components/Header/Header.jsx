import styles from './Header.module.css';
const Header = () => {
    return (
        <div className={styles.header_wrapper}>
            <p>Admin UI</p>
        </div>
    )
}

export default Header;