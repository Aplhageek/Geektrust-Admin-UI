import styles from './Header.module.css';
const Header = () => {
    return (
        <header className={styles.header_wrapper}>
            <p>Admin UI</p>
        </header>
    )
}

export default Header;